#route_id: (идентификатор пути. при загрузке преобразуется в "<название плагина>_<исходное значение>")
#    path: test/{var<\d+>?1} (путь. при загрузке преобразуется в "public/plugin/<название плагина>/<исходное значение>")
#    controller: ControllerClass::method (название класса. класс ищется только в неймспейсе \Plugin\<плагин>\public_controllers)
#    methods: GET|POST|PUT (методы)
