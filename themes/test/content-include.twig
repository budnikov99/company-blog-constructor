{% if data %}
    {% if data.type == 'static'%}
        {{include('content/static.html.twig')}}
    {% elseif data.type == 'post' %}
        {{include('content/post.html.twig')}}
    {% elseif data.type == 'posts' %}
        TODO: default theme post list by category display
    {% endif %}
{% else %}
    No content.
{% endif %}