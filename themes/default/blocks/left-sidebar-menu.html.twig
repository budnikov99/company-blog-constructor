<div class="left-sidebar__menu left-sidebar__block">
    {% for item in data.items %}
        <div class="left-sidebar__menu__item">
            {% if item.children is defined %}
                <div class="left-sidebar__submenu">
                    <div class="left-sidebar__menu left-sidebar__block">
                        {% for child in item.children %}
                            <div class="left-sidebar__menu__item">
                                {% if child.url == null %}
                                    <span class="menu-item-text">{{child.text|e}}</span>
                                {% else %}
                                    <a class="menu-item-text" href="{{child.url}}">{{child.text|e}}</a>
                                {% endif %}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}
            {% if item.url == null %}
                <span class="menu-item-text">{{item.text|e}}</span>
            {% else %}
                <a class="menu-item-text" href="{{item.url}}">{{item.text|e}}</a>
            {% endif %}
        </div>
    {% endfor %}
</div>